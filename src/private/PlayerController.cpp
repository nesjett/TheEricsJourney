#include <PlayerController.h>

PlayerController::PlayerController(){
    prueba = true;
}

void PlayerController::Update(sf::Vector2f pos){
    
    
    
    /*if(teclita==sf::Keyboard::Up || teclita==sf::Keyboard::W){
        jugador->direction = Vector2f(0.0,-0.1); // Moverse hacia arriba
    }
    if(teclita==sf::Keyboard::Down || teclita==sf::Keyboard::S){
        jugador->direction = Vector2f(0.0, 0.1);
    }
    if(teclita==sf::Keyboard::Left || teclita==sf::Keyboard::A){
        jugador->direction = Vector2f(-0.1, 0.0);
    }
    if(teclita==sf::Keyboard::Right || teclita==sf::Keyboard::D){
        jugador->direction = Vector2f(0.1, 0.0);
    }
    else{
        jugador->direction = Vector2f(0.0, 0.0);
    }*/
    
}

PlayerController::~PlayerController() // Destructor
{
 
}